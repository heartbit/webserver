define('information', ['config', 'moment', 'backbone'], function(config, moment, Backbone) {

	var Information = Backbone.Model.extend({

		initialize: function() {},

	});

	return Information;
	
});